from configurations.global_config import GlobalConfig


class ExecutionParameters:

    # ticker
    TICKER = GlobalConfig.PALANTIR_TICKER_STR

    # time interval
    INTERVAL = GlobalConfig.ONE_MIN_INTERVAL
